import {
  OPTIONS as validateOptions,
  POST as validatePost,
} from '../../license/validate/route'

export const dynamic = 'force-dynamic'
export const OPTIONS = validateOptions
export const POST = validatePost
